{
  "name": "amravti-fp-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Amravti FP backend API — run from this folder",
  "scripts": {
    "prestart": "bash ../free-ports.sh 3001",
    "predev": "bash ../free-ports.sh 3001",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  },
  "dependencies": {
    "nodemailer": "^9.0.3",
    "pdfkit": "^0.19.1"
  }
}
